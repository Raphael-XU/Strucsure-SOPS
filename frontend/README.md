# Frontend (Part 1)

React app (`react-scripts`), Tailwind, and static assets under `public/`.

- **Owner (integration push):** Joshua Balista  
- From repo root: `npm run install:all` then `npm start`.  
- From this folder: `npm start`, `npm run build`.

See the root [README](../README.md) for the full four-part layout.
