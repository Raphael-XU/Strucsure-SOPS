# Firebase rules & indexes (Part 3)

This folder holds **Firestore** rules and indexes and **Cloud Storage** rules for Strucsure SOPS.

- **Owner (integration push):** Cholo Diel  
- Deploy with Firebase CLI from the **repository root** (paths are set in `firebase.json`).

See the root [README](../README.md) for the full four-part layout.
