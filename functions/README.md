# Cloud Functions (Part 2)

Firebase Cloud Functions (Node 18). Entry: `index.js`.

- **Owner (integration push):** Jhemar Visande  
- From repo root: `npm install --prefix functions` then deploy with `firebase deploy --only functions` (after Firebase login).

See the root [README](../README.md) for the full four-part layout.
